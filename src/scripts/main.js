const ws = new WebSlides({
    autoslide: false,
    changeOnClick: false,
    loop: true,
    minWheelDelta: 40,
    navigateOnScroll: true,
    scrollWait: 450,
    slideOffset: 50,
    showIndex: true
  });